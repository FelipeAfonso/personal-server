# Codex

Use native workers only when permitted by the active session. For external
Claude workers, follow `~/.agents/references/delegation.md`. Check the models
actually exposed by the runtime before selecting one.
