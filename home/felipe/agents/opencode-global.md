# OpenCode

Check available providers before selecting a worker. Follow
`~/.agents/references/delegation.md` when native workers cannot reach the
preferred model; do not silently substitute an unlisted model.
